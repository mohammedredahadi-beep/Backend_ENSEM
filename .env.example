# ENSEM ACCESS – Backend Environment Variables
PORT=3001
FRONTEND_URL=http://localhost:5173
FROM_EMAIL=noreply@ensem.ac.ma

# SMTP (production uniquement)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=your_sendgrid_api_key

# PostgreSQL (production)
# DATABASE_URL=postgresql://ensem:changeme@localhost:5432/ensem_access

# Redis (production)
# REDIS_URL=redis://localhost:6379
